dependencies:
  flutter:
    sdk: flutter
  nfc_manager: ^3.6.0
  encrypt: ^5.0.0
  shared_preferences: ^2.2.0
  intl: ^0.18.0
  crypto: ^3.0.0
  local_auth: ^2.1.0              # Biometric auth
  qr_flutter: ^4.0.0              # QR codes
  http: ^1.1.0                    # Server sync
  flutter_secure_storage: ^9.0.0  # Secure key storage
