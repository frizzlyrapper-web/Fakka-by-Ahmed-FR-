name: fakka_app
description: Fakka Currency and NFC App
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  nfc_manager: ^3.3.0 # مكتبة الـ NFC اللي بيحتاجها كودك

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
